import { CdsIcon } from "@cds/core/icon";

export interface SgxIconI {
  shape: CdsIcon['shape'];
  size?: CdsIcon['size'];
  direction?: CdsIcon['direction'];
  flip?: CdsIcon['flip'];
  solid?: CdsIcon['solid'];
  status?: CdsIcon['status'];
  inverse?: CdsIcon['inverse'];
  badge?: CdsIcon['badge'];
  title?: string;
}
