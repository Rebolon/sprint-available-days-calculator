import { TeammateFormTitlePipe } from './teammate-form-title.pipe';

describe('TeammateFormTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TeammateFormTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
